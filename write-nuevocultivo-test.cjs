const fs = require('fs')

const lines = []
lines.push("import { describe, it, expect, vi } from 'vitest'")
lines.push("import { render, screen, fireEvent } from '@testing-library/react'")
lines.push("import '@testing-library/jest-dom'")
lines.push("import { NuevoCultivoForm } from './NuevoCultivoForm'")
lines.push("")
lines.push("const mockGeneticas = [")
lines.push("  { id: 'g1', nombre: 'Tropical Runtz' },")
lines.push("  { id: 'g2', nombre: 'Carlson Phenotype A' },")
lines.push("] as const")
lines.push("")
lines.push("describe('NuevoCultivoForm', () => {")
lines.push("  it('muestra los campos basicos del formulario', () => {")
lines.push("    render(")
lines.push("      <NuevoCultivoForm")
lines.push("        geneticasDisponibles={mockGeneticas}")
lines.push("        onGuardar={vi.fn()}")
lines.push("        onCargarGenetica={vi.fn()}")
lines.push("      />")
lines.push("    )")
lines.push("    expect(screen.getByLabelText(/nombre/i)).toBeInTheDocument()")
lines.push("    expect(screen.getByLabelText(/etapa/i)).toBeInTheDocument()")
lines.push("    expect(screen.getByLabelText(/fecha de inicio/i)).toBeInTheDocument()")
lines.push("    expect(screen.getByLabelText(/cantidad de plantas/i)).toBeInTheDocument()")
lines.push("  })")
lines.push("")
lines.push("  it('muestra el selector de genetica con las opciones disponibles', () => {")
lines.push("    render(")
lines.push("      <NuevoCultivoForm")
lines.push("        geneticasDisponibles={mockGeneticas}")
lines.push("        onGuardar={vi.fn()}")
lines.push("        onCargarGenetica={vi.fn()}")
lines.push("      />")
lines.push("    )")
lines.push("    expect(screen.getByLabelText(/genetica/i)).toBeInTheDocument()")
lines.push("    expect(screen.getByText('Tropical Runtz')).toBeInTheDocument()")
lines.push("    expect(screen.getByText('Carlson Phenotype A')).toBeInTheDocument()")
lines.push("  })")
lines.push("")
lines.push("  it('muestra aviso para cargar genetica si la lista esta vacia', () => {")
lines.push("    render(")
lines.push("      <NuevoCultivoForm")
lines.push("        geneticasDisponibles={[]}")
lines.push("        onGuardar={vi.fn()}")
lines.push("        onCargarGenetica={vi.fn()}")
lines.push("      />")
lines.push("    )")
lines.push("    expect(screen.getByText(/tenes que cargar una genetica primero/i)).toBeInTheDocument()")
lines.push("    expect(screen.queryByLabelText(/genetica/i)).not.toBeInTheDocument()")
lines.push("  })")
lines.push("")
lines.push("  it('llama a onCargarGenetica al tocar el boton cuando no hay geneticas', () => {")
lines.push("    const onCargarGenetica = vi.fn()")
lines.push("    render(")
lines.push("      <NuevoCultivoForm")
lines.push("        geneticasDisponibles={[]}")
lines.push("        onGuardar={vi.fn()}")
lines.push("        onCargarGenetica={onCargarGenetica}")
lines.push("      />")
lines.push("    )")
lines.push("    fireEvent.click(screen.getByTestId('boton-cargar-genetica'))")
lines.push("    expect(onCargarGenetica).toHaveBeenCalledTimes(1)")
lines.push("  })")
lines.push("")
lines.push("  it('llama a onGuardar con los datos completos al enviar el formulario', () => {")
lines.push("    const onGuardar = vi.fn()")
lines.push("    render(")
lines.push("      <NuevoCultivoForm")
lines.push("        geneticasDisponibles={mockGeneticas}")
lines.push("        onGuardar={onGuardar}")
lines.push("        onCargarGenetica={vi.fn()}")
lines.push("      />")
lines.push("    )")
lines.push("    fireEvent.change(screen.getByLabelText(/nombre/i), { target: { value: 'Sala Nueva' } })")
lines.push("    fireEvent.change(screen.getByLabelText(/etapa/i), { target: { value: 'vegetativo' } })")
lines.push("    fireEvent.change(screen.getByLabelText(/fecha de inicio/i), { target: { value: '2026-06-18' } })")
lines.push("    fireEvent.change(screen.getByLabelText(/cantidad de plantas/i), { target: { value: '8' } })")
lines.push("    fireEvent.change(screen.getByLabelText(/genetica/i), { target: { value: 'g2' } })")
lines.push("    fireEvent.click(screen.getByTestId('boton-guardar-cultivo'))")
lines.push("    expect(onGuardar).toHaveBeenCalledTimes(1)")
lines.push("    expect(onGuardar).toHaveBeenCalledWith({")
lines.push("      nombre: 'Sala Nueva',")
lines.push("      etapa: 'vegetativo',")
lines.push("      fechaInicio: '2026-06-18',")
lines.push("      cantidadPlantas: 8,")
lines.push("      geneticaId: 'g2',")
lines.push("    })")
lines.push("  })")
lines.push("})")

const code = lines.join('\n')
fs.writeFileSync('src/components/NuevoCultivoForm.test.tsx', code, { encoding: 'utf8' })
console.log('OK - NuevoCultivoForm.test.tsx escrito, ' + lines.length + ' lineas')